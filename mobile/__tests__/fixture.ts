import type { Card, Dashboard } from '../src/types';
export const card: Card = { id: 'test-1', priority: 'P2', lane: 'now', category: 'strategic', title: 'Review the planning proposal', context: 'A decision is needed this afternoon.', consequence: 'Keeps the project moving.', source: 'Planning note', mission_alignment: 'aligned', action: { label: 'Approve the planning review', kind: 'eli_agent_queue', account: 'work', recipients: ['review@example.test'], arguments: { note: 'Review only' }, reversible: true } };
export function dashboard(now = Date.now()): Dashboard { return { generated_at: new Date(now).toISOString(), expires_at: new Date(now + 300_000).toISOString(), timezone: 'America/Los_Angeles', live: true, greeting: 'Your day, in focus.', focus: 'Protect time for the important work.', cards: [card], calendar_items: [], admin_count: 0, integrations: { eli_agent: true }, warnings: [], eli: { healthy: true, checked_at: new Date(now).toISOString(), persona: { stage: 0, autonomy: [{ category: 'calendar', level: 0, clean_streak: 1 }] }, memory: { files: 24, chunks: 100, status: 'ok' } } }; }
